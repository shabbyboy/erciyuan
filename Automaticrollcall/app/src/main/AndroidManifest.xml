<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.automaticrollcall">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Classmanger"
            android:label="名单导入" />
        <activity
            android:name=".Numbermanger"
            android:label="名单管理" />
        <activity
            android:name=".Checkstudent"
            android:label="点名" />
        <activity
            android:name=".Attendancerecord"
            android:label="出勤记录" />
        <activity android:name=".tongzhi"
            android:label="相关通知"/>
        <activity android:name=".tongji"
            android:label="出勤统计"/>
    </application>

</manifest>
